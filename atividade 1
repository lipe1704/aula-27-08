/*Escreva um programa que calcule a soma dos números pares entre 1 e 100*/
#include <stdio.h>
int main(){
    int i, soma=0 ;
    for(int i=1; i<=100; i++){
        if (i%2==0){
            
            soma+=i;
        }
    }
    printf ("A soma dos números pares são: %i", soma);
    return 0;
}
